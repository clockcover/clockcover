{
  "name": "clockcover",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "prepare": "husky",
    "test:guards": "node --test tooling/guards/tests/*.test.ts .claude/hooks/tests/*.test.ts"
  },
  "engines": {
    "node": ">=23.6"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "turbo": "^2.10.12"
  }
}
