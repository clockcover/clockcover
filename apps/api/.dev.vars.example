# Copy to .dev.vars (gitignored, read-denied for the agent) for `wrangler dev`.
# Production values are set with `wrangler secret put <NAME>`.
API_KEY=change-me
LINK_SECRET=change-me-too
RESEND_API_KEY=re_xxxxxxxxx
